

## Update HeroServiceTiles Font to Arial Regular

The text in the hero service tiles (Hajj Packages, Umrah Packages, Visa Services) will be changed to use Arial font with regular (normal) weight.

### Changes

1. **`src/components/HeroServiceTiles.tsx`**
   - Add inline style or Tailwind class to set `font-family: Arial, sans-serif` and `font-weight: normal` on the tile title text elements

This is a small, focused change -- just updating the font family and weight on the tile labels.

